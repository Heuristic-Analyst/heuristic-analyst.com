Chat with AI locally, can use tools now



To create Python Environment, as its definition is written in Docker-compose.yml: docker compose up -d

to start linux bash (docker compose yml service name = dev): docker compose exec -it dev bash

to stop: docker compose stop

to restart again: docker compose start

to stop and delete: docker compose down
